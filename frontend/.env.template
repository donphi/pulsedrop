# NextAuth Configuration
# Required for NextAuth.js authentication flows.
# Set NEXTAUTH_URL to your deployment URL (e.g., http://localhost:3000 or https://yourdomain.com)
NEXTAUTH_URL=
# A secret key used to sign tokens and cookies. Generate a strong random string.
# You can generate one using: openssl rand -base64 32
NEXTAUTH_SECRET=

# Supabase Configuration
# Public URL for your Supabase project. Found in your Supabase project settings.
NEXT_PUBLIC_SUPABASE_URL=
# Public anonymous key for your Supabase project. Found in your Supabase project settings.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key for server-side Supabase operations (e.g., admin tasks). Keep this secret!
# Uncomment and set if you need server-side admin access to Supabase.
SUPABASE_SERVICE_ROLE_KEY=

# Strava API Credentials
# Client ID for your Strava application. Found in your Strava API settings.
STRAVA_CLIENT_ID=
# Client Secret for your Strava application. Found in your Strava API settings. Keep this secret!
STRAVA_CLIENT_SECRET=

# Strava Webhook Configuration
# Verification token for Strava webhook subscription. Create a random string for this.
# This is used to verify that webhook requests are coming from Strava.
STRAVA_VERIFY_TOKEN=
# ID of your Strava webhook subscription. You'll get this after registering your webhook.
STRAVA_WEBHOOK_SUBSCRIPTION_ID=